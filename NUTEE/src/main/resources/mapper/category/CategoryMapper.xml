<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.nutee.repository.mapper.CategoryMapper">

	<select id="findAllByBoardId" resultType="Category">
		SELECT c.* FROM Category c 
		LEFT JOIN Board b ON c.boardId = b.id 
		WHERE c.boardId = #{boardId}
	</select>
	
	<select id="findOne" resultType="Category">
		SELECT * FROM Category
		WHERE id = #{id}
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT Category(categoryName, boardId)
		VALUES(#{categoryName}, #{boardId})
	</insert>
	
	<update id="update">
	  	UPDATE Category
	  	SET categoryName=#{category.categoryName}
	  	WHERE id=#{id}
	</update>
	
	<delete id="delete">
	  	DELETE FROM Category WHERE id=#{id}
	</delete>

</mapper>