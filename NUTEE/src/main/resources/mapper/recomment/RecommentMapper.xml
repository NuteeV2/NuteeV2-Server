<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.nutee.repository.mapper.RecommentMapper">

	<insert id="create" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO COMMENT (contents, userId, anonymous, articleId, recommentId)
		VALUES (#{contents}, #{userId}, #{anonymous}, #{articleId}, #{recommentId});
	</insert>
	
	<update id="update">
		UPDATE COMMENT
		SET contents = #{contents}, userId = #{userId}, anonymous = #{anonymous}, articleId = #{articleId}, recommentId = #{recommentId}
		WHERE id = #{id}
	</update>

	<update id="delete">
		UPDATE COMMENT
		SET deleted = 'Y'
		WHERE id = #{id}
	</update>
	
</mapper>